<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QRScanner" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="android:colorPrimaryDark">@color/dark_grey</item>
        <item name="colorOnPrimary">@color/sage</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/sage</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/sage</item>
        <item name="android:navigationBarColor">@color/sage</item>
        <!-- Customize your theme here. -->
    </style>
</resources>